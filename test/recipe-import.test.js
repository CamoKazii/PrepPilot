import test from'node:test';import assert from'node:assert/strict';import{migrateAuditedRecipe,migrateAuditedRecipes,parseRecipeImport}from'../src/domain/recipes/import.js';
const legacy={id:'B1',title:'Test oats',meal:'Breakfast',servings:5,method:['Cook'],storage:'Chill',reheat:'Microwave',equipment:'Pot',ingredients:[['Oats','500 g']],batchMacros:{calories:2500,protein:150,carbs:300,fat:70},calories:500,protein:30,carbs:60,fat:14,assumption:'Test',textureCheck:'Creamy'};
test('migrates audited recipe without changing displayed macros',()=>{const r=migrateAuditedRecipe(legacy);assert.equal(r.status,'ingredient-verified');assert.deepEqual(r.verification.perServing,{calories:500,protein:30,carbs:60,fat:14});assert.equal(r.immutable,true)});
test('migration detects batch reconciliation errors',()=>{const result=migrateAuditedRecipes([{...legacy,batchMacros:{...legacy.batchMacros,protein:100}}]);assert.equal(result.errors.length,1);assert.equal(result.errors[0].field,'protein')});
test('structured import returns record-level errors',()=>{const result=parseRecipeImport(JSON.stringify([{id:'ok',title:'Recipe',servings:5,ingredients:[]},{id:'bad'}]));assert.equal(result.records.length,1);assert.equal(result.records[0].status,'draft');assert.equal(result.errors.length,1)});
test('structured import rejects malformed JSON',()=>assert.equal(parseRecipeImport('{bad').errors.length,1));
