import React from'react';
export class ErrorBoundary extends React.Component{constructor(props){super(props);this.state={error:null}}static getDerivedStateFromError(error){return{error}}componentDidCatch(error,info){console.error('PrepPilot render error',error,info)}render(){if(!this.state.error)return this.props.children;return <main className="fatal-state" role="alert"><p className="eyebrow">Something went wrong</p><h1>PrepPilot could not render this page.</h1><p>Your browser data has not been deleted. Reload the app or open Data tools to export a backup.</p><div className="actions"><button className="primary" onClick={()=>location.reload()}>Reload app</button><a className="secondary" href="#/data-tools">Open data tools</a></div><details><summary>Technical detail</summary><pre>{String(this.state.error)}</pre></details></main>}}
export function EmptyState({title,children,action}){return <div className="empty" role="status"><h2>{title}</h2>{children&&<p>{children}</p>}{action}</div>}
export function Notice({children,tone='info'}){return <div className={`notice notice-${tone}`} role={tone==='error'?'alert':'status'}>{children}</div>}