import{nextDraft}from'./versioning.js';
export function substituteIngredient(recipe,lineIndex,replacement,reason,now=new Date().toISOString()){if(!recipe.ingredients?.[lineIndex])throw new Error('Ingredient line not found.');const before=recipe.ingredients[lineIndex];const ingredients=recipe.ingredients.map((line,i)=>i===lineIndex?{...replacement}:line);const warnings=[];if(before.state!==replacement.state)warnings.push('Ingredient state changed; recalculate water yield and cooking behaviour.');if(before.productId!==replacement.productId)warnings.push('Product changed; full macro recalculation is required.');if(replacement.flags?.includes('liquid'))warnings.push('Liquid balance may change.');if(replacement.flags?.includes('retained-fat'))warnings.push('Retained cooking fat may change.');if(replacement.flags?.includes('baking-structure'))warnings.push('Baking structure or pan depth may change.');const draft=nextDraft(recipe,{ingredients,culinaryReview:{approved:false,resolutionNotes:''},substitutionAudit:[...(recipe.substitutionAudit||[]),{at:now,lineIndex,before,replacement,reason:reason||'No reason supplied',warnings}]},now);return{recipe:draft,warnings}}
