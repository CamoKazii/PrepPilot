export function runCulinaryChecks(recipe,catalogue){const warnings=[],blockers=[];const lines=(recipe.ingredients||[]).map(line=>({line,product:catalogue.find(x=>x.id===line.productId)})).filter(x=>x.product);const total=lines.reduce((s,x)=>s+Number(x.line.quantity||0),0)||1;const grams=(pattern)=>lines.filter(x=>pattern.test(`${x.product.name} ${x.product.tags||''}`)).reduce((s,x)=>s+Number(x.line.quantity||0),0);if(recipe.isBake){const whey=grams(/whey|protein powder/i),eggWhite=grams(/egg white/i),lowFatDairy=grams(/low-fat|skim|0%|light yoghurt/i),starch=grams(/flour|oat|starch|semolina/i),wholeEgg=grams(/whole egg/i);if(whey/total>0.18)warnings.push('Protein powder exceeds 18% of listed ingredient mass; chalky or dry texture risk.');if(eggWhite>wholeEgg*2&&eggWhite>150)warnings.push('Egg white materially exceeds whole egg; rubbery or custard-like texture risk.');if(whey+eggWhite+lowFatDairy>starch*2.5)warnings.push('High-protein liquid/solid ratio is aggressive relative to starch structure.');const p=recipe.pan||{};if(!p.lengthMm||!p.widthMm||!p.depthMm)blockers.push('Baked recipes require pan length, width and depth.');if(!['metal','glass','ceramic'].includes(p.material))blockers.push('Baking-pan material must be metal, glass or ceramic.');if(!['fan-forced','conventional'].includes(p.ovenMode))blockers.push('Oven mode must be fan-forced or conventional.');if(Number(p.batterDepthMm)>Number(p.depthMm)*0.8)blockers.push('Expected batter depth exceeds 80% of pan depth.');}
if(!recipe.intendedTexture)blockers.push('Intended texture is required.');if(!recipe.donenessCues)blockers.push('Doneness cues are required.');return{warnings,blockers,requiresResolution:warnings.length>0}}
