import React from'react';
export function ServingControl({servings,onChange,canonical=5}){return <section className="serving-control"><div><b>Scale the batch</b><p>Derived from the audited {canonical}-serving recipe. Per-serving macros remain unchanged.</p></div><div className="serving-presets">{[2,3,4,5,6,8,10].map(x=><button className={servings===x?'active':''} key={x} onClick={()=>onChange(x)}>{x}</button>)}<input aria-label="Custom servings" type="number" min="1" max="20" value={servings} onChange={e=>onChange(Math.max(1,Math.min(20,Number(e.target.value)||1)))}/></div></section>}
