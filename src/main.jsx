import React,{useMemo,useState}from'react';
import{createRoot}from'react-dom/client';
import{HashRouter,Link,NavLink,Route,Routes,useParams}from'react-router-dom';
import{bySlug,recipes,targets}from'./data';
import'./styles.css';
const SHOPPING_KEY='preppilot-shopping-v1',PLANNER_KEY='preppilot-planner-v1';
const DAYS=['Monday','Tuesday','Wednesday','Thursday','Friday','Saturday','Sunday'],MEALS=['Breakfast','Lunch','Dinner'];
function readLocal(key,fallback){try{return JSON.parse(localStorage.getItem(key)||JSON.stringify(fallback))}catch{return fallback}}
function writeLocal(key,value){localStorage.setItem(key,JSON.stringify(value))}
function Layout(){return <div className="shell"><aside><Link className="brand" to="/"><span className="mark">P</span><span><b>PrepPilot</b><small>Meal prep, dialled in.</small></span></Link><nav><NavLink to="/">Dashboard</NavLink><NavLink to="/recipes">Recipes</NavLink><NavLink to="/planner">Planner</NavLink><NavLink to="/shopping-list">Shopping list</NavLink></nav><div className="target-note"><span>Daily target</span><b>{targets.calories.toLocaleString('en-AU')} kcal</b><small>≥{targets.protein} P · {targets.carbs} C · {targets.fat} F</small></div></aside><main><Routes><Route path="/" element={<Dashboard/>}/><Route path="/recipes" element={<Library/>}/><Route path="/recipes/:slug" element={<Recipe/>}/><Route path="/planner" element={<Planner/>}/><Route path="/shopping-list" element={<Shopping/>}/><Route path="*" element={<NotFound/>}/></Routes></main></div>}
function Dashboard(){const planner=readLocal(PLANNER_KEY,{}),assigned=Object.values(planner).filter(Boolean).length,fullDays=DAYS.map(day=>MEALS.map(meal=>planner[`${day}-${meal}`]).filter(Boolean)).filter(items=>items.length===3).length,counts=recipes.reduce((a,r)=>(a[r.meal]=(a[r.meal]||0)+1,a),{});return <><header className="hero"><p className="eyebrow">Your nutrition command centre</p><h1>Good evening, Cam.</h1><p>Plan a repeatable week, keep protein mandatory, and turn ingredient-audited recipes into one practical prep workflow.</p><div className="actions"><Link className="primary" to="/planner">Plan this week</Link><Link className="secondary" to="/recipes">Browse recipes</Link></div></header><section className="target-grid">{[['Calories',targets.calories,'kcal'],['Protein',targets.protein,'g minimum'],['Carbohydrate',targets.carbs,'g'],['Fat',targets.fat,'g']].map(([l,v,u])=><article key={l}><span>{l}</span><b>{v}</b><small>{u}</small></article>)}</section><section className="summary-grid"><article><span>Meals planned</span><b>{assigned}</b><small>of 21 slots</small></article><article><span>Complete days</span><b>{fullDays}</b><small>breakfast + lunch + dinner</small></article><article><span>Complete recipes</span><b>{recipes.length}</b><small>full cooking and macro detail</small></article></section><section><div className="section-head"><div><p className="eyebrow">Library</p><h2>Built for Sunday prep</h2></div><Link to="/recipes">View all</Link></div><div className="summary-grid">{MEALS.map(meal=><article key={meal}><span>{meal}</span><b>{counts[meal]||0}</b><small>recipes</small></article>)}</div></section><section><div className="section-head"><div><p className="eyebrow">Start here</p><h2>Featured recipes</h2></div></div><div className="cards">{recipes.slice(0,3).map(r=><Card key={r.id} recipe={r}/>)}</div></section></>}
function Library(){const[query,setQuery]=useState(''),[meal,setMeal]=useState('All');const filtered=useMemo(()=>recipes.filter(r=>(meal==='All'||r.meal===meal)&&`${r.title} ${r.description} ${r.tags.join(' ')}`.toLowerCase().includes(query.toLowerCase())),[query,meal]);return <><header className="page-head"><p className="eyebrow">Recipe library</p><h1>Find your next meal prep.</h1><p>Every recipe includes exact Australian metric ingredients, full method, storage, reheating and ingredient-verified macro totals.</p></header><div className="toolbar"><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search recipes…" aria-label="Search recipes"/><div className="chips">{['All',...MEALS].map(x=><button className={meal===x?'active':''} onClick={()=>setMeal(x)} key={x}>{x}</button>)}</div></div><div className="cards">{filtered.map(r=><Card key={r.id} recipe={r}/>)}</div>{!filtered.length&&<div className="empty">No recipes match those filters.</div>}</>}
function Card({recipe:r}){return <Link className="card" to={`/recipes/${r.slug}`}><div className={`art ${r.meal.toLowerCase()}`}><span>{r.id}</span></div><div className="card-body"><div className="meta"><span>{r.meal}</span><span>Ingredient-verified</span></div><h3>{r.title}</h3><p>{r.description}</p><div className="macros"><b>{r.calories} kcal</b><span>{r.protein} P</span><span>{r.carbs} C</span><span>{r.fat} F</span></div></div></Link>}
function MacroComparison({recipe:r}){return <section className="comparison-grid">{[r.initialEstimate,r.calculatedActual,r.difference].map((x,i)=><article className={i===1?'actual':''} key={x.label}><span>{x.label}</span><b>{x.value}</b><small>{x.detail}</small></article>)}</section>}
function Recipe(){const{slug}=useParams(),r=bySlug(slug),[list,setList]=useState(()=>readLocal(SHOPPING_KEY,[]));if(!r)return <NotFound/>;const added=list.includes(r.id);function toggle(){const next=added?list.filter(id=>id!==r.id):[...list,r.id];setList(next);writeLocal(SHOPPING_KEY,next)}return <><Link className="back" to="/recipes">← Back to recipes</Link><header className="recipe-hero"><div><p className="eyebrow">{r.meal} · {r.id}</p><h1>{r.title}</h1><p>{r.description}</p><div className="badge">Ingredient-verified macros</div></div><div className="hero-macro"><span>Per serving</span><b>{r.calories}</b><small>kcal</small></div></header><div className="recipe-actions"><button className="primary" onClick={toggle}>{added?'Remove from shopping list':'Add to shopping list'}</button></div><section className="stat-grid"><article><span>Servings</span><b>{r.servings}</b></article><article><span>Prep</span><b>{r.prep}</b></article><article><span>Cook</span><b>{r.cook}</b></article><article><span>Equipment</span><b>{r.equipment}</b></article></section><MacroComparison recipe={r}/>{r.textureCheck&&<div className="texture-check"><b>Culinary structure check</b><p>{r.textureCheck}</p></div>}<section className="macro-strip">{[['Calories',r.calories],['Protein',`${r.protein} g`],['Carbs',`${r.carbs} g`],['Fat',`${r.fat} g`]].map(([l,v])=><article key={l}><span>{l}</span><b>{v}</b></article>)}</section><div className="recipe-columns"><section><h2>Ingredients</h2><p className="muted">Australian metric. Raw, dry, drained or packaged state is identified where material.</p><ul className="ingredients">{r.ingredients.map(([n,q])=><li key={n}><span>{n}</span><b>{q}</b></li>)}</ul></section><section><h2>Method</h2><ol className="method">{r.method.map((s,i)=><li key={i}>{s}</li>)}</ol></section></div><section className="care"><article><h3>Storage</h3><p>{r.storage}</p></article><article><h3>Reheating</h3><p>{r.reheat}</p></article><article><h3>Restaurant-style upgrade</h3><p>{r.upgrade}</p></article></section><section className="batch-panel"><div><span>Total batch</span><b>{r.batchMacros.calories.toLocaleString('en-AU')} kcal</b><small>{r.batchMacros.protein} P · {r.batchMacros.carbs} C · {r.batchMacros.fat} F</small></div><div><span>Per serving</span><b>{r.calories} kcal</b><small>{r.protein} P · {r.carbs} C · {r.fat} F</small></div></section><div className="assumption"><b>Calorie sanity check</b><p>{r.sanity}</p></div><div className="assumption"><b>Material assumptions</b><p>{r.assumption}</p></div></>}
function Planner(){const[plan,setPlan]=useState(()=>readLocal(PLANNER_KEY,{}));function assign(key,id){const next={...plan,[key]:id||null};setPlan(next);writeLocal(PLANNER_KEY,next)}function clear(){setPlan({});writeLocal(PLANNER_KEY,{})}return <><header className="page-head"><p className="eyebrow">Weekly planner</p><h1>Build a week that adds up.</h1><p>Select breakfast, lunch and dinner for each day. Daily totals update immediately.</p></header><div className="section-head"><h2>Seven-day plan</h2><button className="secondary" onClick={clear}>Clear plan</button></div><div className="planner-grid">{DAYS.map(day=>{const selected=MEALS.map(meal=>recipes.find(r=>r.id===plan[`${day}-${meal}`])).filter(Boolean),total=selected.reduce((s,r)=>({calories:s.calories+r.calories,protein:s.protein+r.protein,carbs:s.carbs+r.carbs,fat:s.fat+r.fat}),{calories:0,protein:0,carbs:0,fat:0});return <section className="day-card" key={day}><div className="day-head"><h3>{day}</h3><span>{selected.length}/3 meals</span></div>{MEALS.map(meal=>{const key=`${day}-${meal}`;return <label key={key}><span>{meal}</span><select value={plan[key]||''} onChange={e=>assign(key,e.target.value)}><option value="">Choose recipe</option>{recipes.filter(r=>r.meal===meal).map(r=><option key={r.id} value={r.id}>{r.title}</option>)}</select></label>})}<div className="day-macros"><b>{Math.round(total.calories)} kcal</b><span className={total.protein>=targets.protein?'good':''}>{Math.round(total.protein)} P</span><span>{Math.round(total.carbs)} C</span><span>{Math.round(total.fat)} F</span></div></section>})}</div></>}
function Shopping(){const[list,setList]=useState(()=>readLocal(SHOPPING_KEY,[])),selected=recipes.filter(r=>list.includes(r.id)),items=selected.flatMap(r=>r.ingredients.map(([name,quantity])=>({name,quantity,recipe:r.title})));function clear(){setList([]);writeLocal(SHOPPING_KEY,[])}return <><header className="page-head"><p className="eyebrow">Shopping list</p><h1>Your prep, consolidated.</h1><p>Selections stay privately on this device and work offline once installed.</p></header>{selected.length?<><div className="section-head"><h2>{selected.length} recipe{selected.length>1?'s':''}</h2><button className="secondary" onClick={clear}>Clear all</button></div><div className="shopping-recipes">{selected.map(r=><article key={r.id}><b>{r.title}</b><Link to={`/recipes/${r.slug}`}>View</Link></article>)}</div><section><h2>Ingredients</h2><div className="checklist">{items.map((x,i)=><label key={`${x.name}-${i}`}><input type="checkbox"/><span><b>{x.name}</b><small>{x.quantity} · {x.recipe}</small></span></label>)}</div></section></>:<div className="empty"><h2>No recipes added yet.</h2><p>Add recipes from the library to build your prep list.</p><Link className="primary" to="/recipes">Browse recipes</Link></div>}</>}
function NotFound(){return <div className="empty"><h1>That page wandered off.</h1><Link className="primary" to="/">Back to dashboard</Link></div>}
createRoot(document.getElementById('root')).render(<React.StrictMode><HashRouter><Layout/></HashRouter></React.StrictMode>);if('serviceWorker'in navigator)window.addEventListener('load',()=>navigator.serviceWorker.register(`${import.meta.env.BASE_URL}sw.js`).catch(()=>{}));
